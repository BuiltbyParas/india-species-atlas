[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback (also provided by public/_redirects)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
